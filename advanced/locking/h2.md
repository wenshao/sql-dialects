# H2 Database: 锁机制 (Locking)

> 参考资料:
> - [H2 Documentation - Advanced Topics: MVCC](https://www.h2database.com/html/advanced.html#mvcc)
> - [H2 Documentation - SQL Grammar: SELECT FOR UPDATE](https://www.h2database.com/html/grammar.html#select)
> - [H2 Documentation - SQL Grammar: SET LOCK_TIMEOUT](https://www.h2database.com/html/commands.html#set_lock_timeout)
> - ============================================================
> - 行级锁
> - ============================================================
> - SELECT FOR UPDATE

```sql
SELECT * FROM orders WHERE id = 100 FOR UPDATE;
```

## FOR UPDATE NOWAIT（H2 2.0+）

```sql
SELECT * FROM orders WHERE id = 100 FOR UPDATE NOWAIT;
```

## 表级锁


## H2 使用表级别的排他锁进行写操作（MVCC 模式下使用行级锁）

没有显式 LOCK TABLE 语句

## MVCC 模式


H2 2.0+ 默认使用 MVCC
旧版本需要在连接 URL 中启用:
jdbc:h2:mem:test;MVCC=TRUE
MVCC 模式下读不阻塞写，写不阻塞读

## 隔离级别


```sql
SET SESSION CHARACTERISTICS AS TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;
SET SESSION CHARACTERISTICS AS TRANSACTION ISOLATION LEVEL READ COMMITTED;
SET SESSION CHARACTERISTICS AS TRANSACTION ISOLATION LEVEL REPEATABLE READ;
SET SESSION CHARACTERISTICS AS TRANSACTION ISOLATION LEVEL SERIALIZABLE;  -- 默认（MVCC 模式下为 READ COMMITTED）
```

## 锁超时


## 设置锁超时（毫秒）

```sql
SET LOCK_TIMEOUT 10000;  -- 默认值取决于配置
```

## 乐观锁


```sql
ALTER TABLE orders ADD COLUMN version INT DEFAULT 1 NOT NULL;

UPDATE orders SET status = 'shipped', version = version + 1
WHERE id = 100 AND version = 5;
```

## 悲观锁


```sql
SET AUTOCOMMIT FALSE;
SELECT * FROM accounts WHERE id = 1 FOR UPDATE;
UPDATE accounts SET balance = balance - 100 WHERE id = 1;
UPDATE accounts SET balance = balance + 100 WHERE id = 2;
COMMIT;
```

## 锁监控


## 查看会话信息

```sql
SELECT * FROM INFORMATION_SCHEMA.SESSIONS;
```

## 查看锁信息

```sql
SELECT * FROM INFORMATION_SCHEMA.LOCKS;
```

## 查看运行中的查询

```sql
SELECT * FROM INFORMATION_SCHEMA.QUERY_STATISTICS;
```

## 注意事项


## H2 2.0+ 默认 MVCC 模式

## 不支持 LOCK TABLE

## 不支持 advisory locks

## 不支持 SKIP LOCKED

## 适合嵌入式和测试场景

## 兼容模式 (MODE=PostgreSQL/MySQL) 不影响锁行为
