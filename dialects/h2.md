# H2

**分类**: Java 嵌入式数据库
**文件数**: 51 个 SQL 文件
**总行数**: 4015 行

## 概述与定位

H2 是一个用纯 Java 编写的开源关系型数据库，由 Thomas Mueller（HSQLDB 的原作者）开发。H2 的核心定位是轻量级嵌入式数据库，特别适合 Java 应用的单元测试、原型开发和嵌入式场景。它同时支持嵌入模式（应用进程内运行）和服务器模式（独立进程），支持内存数据库和磁盘持久化。H2 的一大亮点是提供多种兼容模式——可以模拟 MySQL、PostgreSQL、Oracle、SQL Server 等数据库的行为。

## 历史与演进

- **2004 年**：Thomas Mueller 启动 H2 项目，目标改进 HSQLDB 的设计。
- **2005 年**：H2 1.0 发布，提供嵌入式和服务器两种模式。
- **2011 年**：引入 MVCC 存储引擎提升并发性能。
- **2019 年**：2.0 版本开始大规模重构，引入新存储引擎（MVStore）。
- **2021 年**：2.x 版本稳定化，改进兼容模式和 SQL 标准支持。
- **2023-2025 年**：持续维护更新，保持作为 Java 生态首选嵌入式数据库的地位。

## 核心设计思路

H2 的设计追求**极简和高效**：整个数据库打包为一个约 2.5 MB 的 JAR 文件。存储引擎使用 MVStore（Multi-Version Store），一种基于日志结构的 B-Tree 变体，支持 MVCC 并发控制。H2 提供**多兼容模式**——通过 `MODE=MySQL`、`MODE=PostgreSQL`、`MODE=Oracle`、`MODE=MSSQLServer` 等参数切换，调整 SQL 解析和函数行为以模拟目标数据库。内置 Web 控制台提供浏览器交互界面。

## 独特特色

- **多兼容模式**：`jdbc:h2:mem:test;MODE=MySQL` 启动时指定兼容模式，模拟不同数据库的 SQL 行为。
- **内存 + 文件双模式**：`jdbc:h2:mem:` 纯内存数据库用于测试，`jdbc:h2:file:./data` 持久化到磁盘。
- **CSVREAD/CSVWRITE**：`SELECT * FROM CSVREAD('data.csv')` 直接查询 CSV 文件作为表。
- **LINK TABLE**：`CREATE LINKED TABLE` 连接外部数据库（通过 JDBC）实现联邦查询。
- **单文件部署**：整个数据库引擎是一个 JAR 文件，零外部依赖。
- **Web 控制台**：内置 H2 Console（Web 界面），无需安装客户端工具。
- **全文搜索**：内置基于 Lucene 或原生实现的全文索引。

## 已知不足

- 不适合生产环境的高并发 OLTP 场景——并发写入性能有限。
- 兼容模式虽覆盖常见场景但并非完美，复杂 SQL 可能表现不一致。
- 单文件存储引擎的最大数据量受限（TB 级数据场景不适用）。
- 缺乏生产级的备份恢复、复制和高可用方案。
- 社区规模小，主要由少数核心开发者维护。
- 存储过程和触发器使用 Java 类实现，与标准 SQL 过程语言差异大。

## 对引擎开发者的参考价值

H2 是学习数据库内核实现的理想参考——纯 Java 单代码库实现了完整的 SQL 数据库（解析器、优化器、存储引擎、事务管理），代码可读性好。多兼容模式的实现展示了如何在同一引擎中通过配置切换 SQL 方言行为（函数映射、类型转换规则、分页语法等）。MVStore 存储引擎的设计也是一个清晰的 MVCC B-Tree 实现范例。CSVREAD 展示了如何将外部文件透明映射为关系表。

## 全部模块

### DDL — 数据定义

| 模块 | 特色与分析 |
|---|---|
| [建表](../ddl/create-table/h2.sql) | **Java 嵌入式数据库——内存/文件双模式 + 多兼容模式**。`jdbc:h2:mem:test` 纯内存数据库（测试首选），`jdbc:h2:file:./data` 持久化磁盘。`MODE=MySQL`/`MODE=PostgreSQL`/`MODE=Oracle` 切换 SQL 方言行为。对比 SQLite（C 嵌入式）和 Derby（Java 嵌入式），H2 的多兼容模式使其成为 Java 单元测试中模拟不同数据库行为的最佳选择。 |
| [改表](../ddl/alter-table/h2.sql) | **ALTER 标准支持，内存模式下瞬时执行**——ADD/DROP/ALTER COLUMN 标准语法。内存数据库中 DDL 无磁盘 I/O 延迟。对比 PostgreSQL（ALTER 标准）和 Derby（ALTER 功能有限），H2 的 ALTER 操作在内存模式下零延迟。 |
| [索引](../ddl/indexes/h2.sql) | **B-tree/Hash 索引 + 全文索引（Lucene 集成）**——内置原生全文搜索或通过 Apache Lucene 集成提供更强大的全文检索能力。Hash 索引适合等值查询。对比 PostgreSQL 的 GIN/GiST（类型更丰富）和 Derby（仅 B-tree），H2 的 Lucene 集成是嵌入式数据库中最完整的全文搜索方案。 |
| [约束](../ddl/constraints/h2.sql) | **PK/FK/CHECK/UNIQUE 完整支持且强制执行**。兼容模式下约束行为会调整以匹配目标数据库（如 MySQL 模式下 CHECK 行为可能不同）。对比 PostgreSQL（约束强制执行）和 MySQL 8.0（CHECK 从 8.0.16 真正生效），H2 在标准模式下约束行为正确。 |
| [视图](../ddl/views/h2.sql) | **普通视图支持，无物化视图**——视图定义存储在内存/文件中。对比 PostgreSQL（物化视图原生）和 Derby（普通视图），H2 缺少物化视图但对测试场景影响不大。 |
| [序列与自增](../ddl/sequences/h2.sql) | **IDENTITY/SEQUENCE 标准 + AUTO_INCREMENT 兼容**——MySQL 模式下 AUTO_INCREMENT 可用，标准模式下 GENERATED ALWAYS AS IDENTITY。对比 PostgreSQL 的 SERIAL/IDENTITY 和 MySQL 的 AUTO_INCREMENT，H2 按兼容模式适配不同的自增语法。 |
| [数据库/Schema/用户](../ddl/users-databases/h2.sql) | **Schema 支持 + 内置用户权限 + 多模式兼容**——支持多 Schema 命名空间。内置用户认证（无需外部认证系统）。兼容模式影响 Schema 默认行为。对比 PostgreSQL（多 Schema 灵活）和 Derby（Schema=用户），H2 的 Schema 模型简洁但功能完整。 |

### Advanced — 高级特性

| 模块 | 特色与分析 |
|---|---|
| [动态 SQL](../advanced/dynamic-sql/h2.sql) | **无存储过程内动态 SQL——Java UDF 替代**。H2 无内置过程语言，动态逻辑通过 Java 类实现。对比 PostgreSQL 的 PL/pgSQL EXECUTE 和 Oracle 的 EXECUTE IMMEDIATE，H2 将过程化能力委托给 Java 宿主语言。 |
| [错误处理](../advanced/error-handling/h2.sql) | **无过程式错误处理——Java 异常机制**。SQL 执行错误通过 JDBC 的 SQLException 传播到 Java 应用层处理。对比 PostgreSQL 的 EXCEPTION WHEN 和 SQL Server 的 TRY...CATCH，H2 的错误处理完全依赖宿主语言。 |
| [执行计划](../advanced/explain/h2.sql) | **EXPLAIN ANALYZE 标准**——显示查询执行计划和实际行数统计。内存模式下所有操作在内存中执行，I/O 信息有限。对比 PostgreSQL 的 EXPLAIN ANALYZE（最详细）和 MySQL 的 EXPLAIN，H2 的执行计划提供基本信息。 |
| [锁机制](../advanced/locking/h2.sql) | **MVCC（MVStore 引擎）+ 表级锁（旧模式）**——MVStore 存储引擎支持 MVCC 并发控制，读写互不阻塞。旧版 PageStore 使用表级锁。对比 PostgreSQL（行级 MVCC 原生）和 Derby（行级锁+表级锁），H2 的 MVStore MVCC 是嵌入式数据库中较先进的并发控制。 |
| [分区](../advanced/partitioning/h2.sql) | **无分区支持**——定位为嵌入式/测试数据库，大数据量场景不适用。对比 PostgreSQL（声明式分区完整）和 Derby（同样无分区），分区缺失对 H2 的目标场景影响不大。 |
| [权限](../advanced/permissions/h2.sql) | **GRANT/REVOKE 标准 + 用户/角色**——内置用户管理，支持对象级权限控制。对比 PostgreSQL（RBAC 完整）和 Derby（内置身份验证），H2 的权限模型简洁但功能足够。 |
| [存储过程](../advanced/stored-procedures/h2.sql) | **Java/JavaScript UDF——无 PL/SQL 类过程语言**。`CREATE ALIAS func FOR 'pkg.Class.method'` 将 Java 方法注册为 SQL 函数。对比 PostgreSQL 的 PL/pgSQL（内置完整过程语言）和 Derby（Java 存储过程类似），H2 通过 Java 类实现扩展，无内置 SQL 过程语言。 |
| [临时表](../advanced/temp-tables/h2.sql) | **CREATE TEMP TABLE 会话级**——会话结束时自动删除。内存模式下临时表与普通表性能无差异。对比 PostgreSQL（CREATE TEMP TABLE 标准）和 Oracle 的 GTT，H2 的临时表行为简洁标准。 |
| [事务](../advanced/transactions/h2.sql) | **ACID 完整 + MVCC + Savepoint**——MVStore 引擎提供完整 ACID 事务支持。Savepoint 支持部分回滚。对比 PostgreSQL（ACID 完整）和 Derby（ACID 完整），H2 在事务能力上对齐生产级数据库。 |
| [触发器](../advanced/triggers/h2.sql) | **BEFORE/AFTER/INSTEAD OF 触发器（Java 实现）**——触发器通过 Java 类实现（实现 org.h2.api.Trigger 接口）。对比 PostgreSQL（PL/pgSQL 编写触发器）和 MySQL（SQL 编写触发器），H2 的触发器需要编写 Java 代码，门槛稍高但灵活性强。 |

### DML — 数据操作

| 模块 | 特色与分析 |
|---|---|
| [删除](../dml/delete/h2.sql) | **DELETE 标准 + TRUNCATE 即时**——内存模式下删除零延迟。对比各主流引擎 DELETE 行为一致。 |
| [插入](../dml/insert/h2.sql) | **INSERT 标准 + MERGE（UPSERT）支持**——H2 的 MERGE 语法支持简洁的 Upsert 操作。对比 PostgreSQL 的 ON CONFLICT 和 MySQL 的 ON DUPLICATE KEY UPDATE，H2 的 MERGE 在测试场景下足够使用。 |
| [更新](../dml/update/h2.sql) | **UPDATE 标准**——标准 UPDATE SET WHERE 语法。对比各主流引擎 UPDATE 行为一致。 |
| [Upsert](../dml/upsert/h2.sql) | **MERGE 标准语法（H2 扩展）**——`MERGE INTO t KEY(id) VALUES (1, 'x')` 根据 KEY 列自动判断插入或更新。兼容模式下行为可能调整以匹配目标数据库的 Upsert 语法。对比 PostgreSQL 的 ON CONFLICT（需指定约束）和 SQL 标准 MERGE（语法更复杂），H2 的 MERGE KEY 语法是最简洁的 Upsert 实现。 |

### Functions — 内置函数

| 模块 | 特色与分析 |
|---|---|
| [聚合函数](../functions/aggregate/h2.sql) | **标准聚合函数 + LISTAGG/STRING_AGG**——兼容模式下使用对应数据库的聚合函数名称。对比 PostgreSQL 的 string_agg 和 Oracle 的 LISTAGG，H2 两个函数名都可用。 |
| [条件函数](../functions/conditional/h2.sql) | **CASE/COALESCE/NULLIF + NVL/DECODE（多兼容模式）**——Oracle 模式下 NVL/DECODE 可用，MySQL 模式下 IF/IFNULL 可用。对比 PostgreSQL（无 NVL/DECODE）和 Oracle（DECODE 原生），H2 的兼容模式使测试中可复用目标数据库的函数。 |
| [日期函数](../functions/date-functions/h2.sql) | **标准日期函数——兼容模式影响行为**。MySQL 模式下 DATE_FORMAT/STR_TO_DATE 可用，PG 模式下 date_trunc/extract 可用。对比各目标数据库的日期函数，H2 通过兼容模式模拟目标行为但并非 100% 覆盖。 |
| [数学函数](../functions/math-functions/h2.sql) | **完整数学函数**——MOD/CEIL/FLOOR/ROUND/POWER/SQRT 标准集合。对比各主流引擎数学函数基本一致。 |
| [字符串函数](../functions/string-functions/h2.sql) | **\|\| 拼接 + CONCAT——兼容模式影响函数名**。MySQL 模式下 CONCAT 为主（\|\| 可能被解释为 OR），PG 模式下 \|\| 为主。对比 MySQL（CONCAT 函数）和 PostgreSQL（\|\| 运算符），H2 按兼容模式切换拼接行为。 |
| [类型转换](../functions/type-conversion/h2.sql) | **CAST 标准**——兼容模式下隐式转换规则跟随目标数据库。对比 PostgreSQL（严格类型）和 MySQL（宽松转换），H2 的类型转换行为随模式变化。 |

### Query — 查询

| 模块 | 特色与分析 |
|---|---|
| [CTE](../query/cte/h2.sql) | **WITH + 递归 CTE 标准支持**——递归 CTE 可用于层级查询和序列生成。对比 PostgreSQL（WITH RECURSIVE 标准）和 Derby（不支持 CTE），H2 的 CTE 能力完整。 |
| [全文搜索](../query/full-text-search/h2.sql) | **内置全文搜索（Lucene 集成）+ FULLTEXT INDEX**——原生全文搜索或 Lucene 引擎二选一。`CREATE FULLTEXT INDEX` 创建全文索引，`FT_SEARCH` 函数查询。对比 PostgreSQL 的 tsvector+GIN（内置最成熟）和 Derby（无全文搜索），H2 的 Lucene 集成在嵌入式数据库中独树一帜。 |
| [连接查询](../query/joins/h2.sql) | **JOIN 完整 + NATURAL JOIN**——INNER/LEFT/RIGHT/FULL/CROSS/NATURAL JOIN 均支持。对比 PostgreSQL（完整 JOIN + LATERAL）和 Derby（无 FULL JOIN 限制），H2 的 JOIN 功能完整。 |
| [分页](../query/pagination/h2.sql) | **LIMIT/OFFSET + FETCH FIRST 双支持**——标准 LIMIT/OFFSET 语法和 SQL:2008 FETCH FIRST 语法均可用。兼容模式下分页语法跟随目标数据库。对比 MySQL 的 LIMIT 和 Db2 的 FETCH FIRST，H2 两种分页语法都支持。 |
| [行列转换](../query/pivot-unpivot/h2.sql) | **无原生 PIVOT**——需使用 CASE+GROUP BY 模拟。对比 Oracle（PIVOT 原生）和 BigQuery（PIVOT 原生），H2 缺少行列转换语法。 |
| [集合操作](../query/set-operations/h2.sql) | **UNION/INTERSECT/EXCEPT 完整**——ALL/DISTINCT 修饰符支持。对比 PostgreSQL（集合操作完整）和 Derby（集合操作完整），H2 的集合操作功能标准。 |
| [子查询](../query/subquery/h2.sql) | **关联子查询 + IN/EXISTS 标准**——优化器可将部分子查询转为 JOIN。对比 PostgreSQL（优化器更成熟）和 MySQL 8.0（子查询优化改善），H2 的子查询功能完整。 |
| [窗口函数](../query/window-functions/h2.sql) | **完整窗口函数支持（1.4+）**——ROW_NUMBER/RANK/DENSE_RANK/LAG/LEAD + ROWS/RANGE 帧。对比 PostgreSQL（窗口函数完整）和 Derby（窗口函数有限），H2 的窗口函数功能完整，使其成为测试窗口函数逻辑的理想嵌入式数据库。 |

### Scenarios — 实战场景

| 模块 | 特色与分析 |
|---|---|
| [日期填充](../scenarios/date-series-fill/h2.sql) | **SYSTEM_RANGE(start, end) 生成序列（H2 独有函数）**——`SELECT * FROM SYSTEM_RANGE(1, 365)` 生成整数序列，再转为日期。对比 PostgreSQL 的 generate_series（更直观）和 BigQuery 的 GENERATE_DATE_ARRAY，H2 的 SYSTEM_RANGE 是嵌入式数据库中的便捷序列生成器。 |
| [去重](../scenarios/deduplication/h2.sql) | **ROW_NUMBER + CTE 去重**——标准窗口函数去重模式。对比 PostgreSQL 的 DISTINCT ON（更简洁）和 BigQuery 的 QUALIFY，H2 使用通用去重方案。 |
| [区间检测](../scenarios/gap-detection/h2.sql) | **SYSTEM_RANGE + 窗口函数**——SYSTEM_RANGE 生成完整序列，LEFT JOIN 检测缺失。对比 PostgreSQL 的 generate_series（类似）和 Firebird（递归 CTE），H2 的 SYSTEM_RANGE 方案更简洁。 |
| [层级查询](../scenarios/hierarchical-query/h2.sql) | **递归 CTE 标准**——WITH RECURSIVE 实现层级遍历。对比 PostgreSQL（相同方案）和 Oracle（CONNECT BY），H2 的递归 CTE 功能标准。 |
| [JSON 展开](../scenarios/json-flatten/h2.sql) | **JSON 函数支持（2.0+）**——2.0 版本引入 JSON 类型和 JSON 函数。对比 PostgreSQL 的 JSONB+GIN（功能最强）和 Derby（无 JSON），H2 2.0 的 JSON 支持使其在测试场景中更实用。 |
| [迁移速查](../scenarios/migration-cheatsheet/h2.sql) | **Java 嵌入式 + 兼容模式（PG/MySQL/Oracle/SQL Server）降低测试迁移成本**。关键注意：兼容模式并非 100% 完美模拟——复杂 SQL 和边界行为可能与目标数据库不同；适合单元测试和原型验证，不适合生产环境；2.5 MB JAR 文件零依赖。 |
| [TopN 查询](../scenarios/ranking-top-n/h2.sql) | **ROW_NUMBER + LIMIT 标准**——标准 TopN 方案。内存模式下 TopN 排序极快。对比 PostgreSQL（相同方案）和 BigQuery（QUALIFY），H2 使用通用 TopN 方案。 |
| [累计求和](../scenarios/running-total/h2.sql) | **SUM() OVER 标准**——标准窗口累计。内存模式下计算极快。对比各主流引擎写法一致。 |
| [缓慢变化维](../scenarios/slowly-changing-dim/h2.sql) | **MERGE 标准实现**——MERGE KEY 语法简洁实现 SCD Type 1。对比 PostgreSQL 的 ON CONFLICT 和 Oracle 的 MERGE，H2 的 MERGE KEY 在测试中使用方便。 |
| [字符串拆分](../scenarios/string-split-to-rows/h2.sql) | **无原生拆分展开函数**——需用递归 CTE 或 Java UDF 实现。对比 PostgreSQL 的 string_to_array+unnest（一行搞定）和 BigQuery 的 SPLIT+UNNEST，H2 在字符串拆分上较为不便。 |
| [窗口分析](../scenarios/window-analytics/h2.sql) | **完整窗口函数（1.4+）**——移动平均、排名、占比等分析全覆盖。使 H2 成为测试窗口函数 SQL 逻辑的理想数据库。对比 PostgreSQL（功能对等）和 Derby（窗口函数有限），H2 的窗口函数完整性是其作为测试数据库的重要优势。 |

### Types — 数据类型

| 模块 | 特色与分析 |
|---|---|
| [复合类型](../types/array-map-struct/h2.sql) | **ARRAY 类型支持，无 STRUCT/MAP**——支持 `INTEGER ARRAY` 数组列。对比 PostgreSQL（ARRAY 原生）和 Derby（无 ARRAY），H2 的 ARRAY 支持使其在测试中能覆盖 PG 的数组场景。 |
| [日期时间](../types/datetime/h2.sql) | **DATE/TIME/TIMESTAMP/TIMESTAMP WITH TIME ZONE 标准**——时间类型遵循 SQL 标准定义。兼容模式下行为可能调整（如 MySQL 模式下 DATETIME/TIMESTAMP 区分）。对比 PostgreSQL（TIMESTAMPTZ 推荐）和 MySQL（DATETIME/TIMESTAMP 经典区分），H2 按兼容模式模拟目标行为。 |
| [JSON](../types/json/h2.sql) | **JSON 类型（2.0+）+ JSON 函数**——2.0 版本引入原生 JSON 类型和 JSON_VALUE/JSON_QUERY 等标准函数。对比 PostgreSQL 的 JSONB（功能最强）和 Derby（无 JSON），H2 2.0 的 JSON 支持满足基本测试需求。 |
| [数值类型](../types/numeric/h2.sql) | **INT/BIGINT/DECIMAL/FLOAT/DOUBLE 标准**——兼容模式下类型别名跟随目标数据库（如 MySQL 模式下 MEDIUMINT 可用）。对比 PostgreSQL（NUMERIC 任意精度）和 MySQL（完整整数类型体系），H2 的数值类型在标准模式下简洁，兼容模式下可扩展。 |
| [字符串类型](../types/string/h2.sql) | **VARCHAR/CHAR/CLOB 标准**——VARCHAR 最大长度 1GB，CLOB 存储大文本。兼容模式下 TEXT 别名可用。对比 PostgreSQL 的 TEXT（无长度限制）和 MySQL 的 VARCHAR(n)，H2 的字符串类型标准且灵活。 |
