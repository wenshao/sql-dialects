# H2 Database: 日期序列生成与间隙填充 (Date Series Fill)

> 参考资料:
> - [H2 Database Documentation](https://h2database.com/html/functions-aggregate.html)
> - [H2 Database Documentation](https://h2database.com/html/grammar.html)


## 准备数据


```sql
CREATE TABLE daily_sales (sale_date DATE PRIMARY KEY, amount DECIMAL(10,2));
```

## H2 特有：SYSTEM_RANGE 生成日期序列


```sql
SELECT DATEADD('DAY', x - 1, DATE '2024-01-01') AS d
FROM SYSTEM_RANGE(1, 10);

SELECT DATEADD('DAY', x - 1, DATE '2024-01-01') AS date,
       COALESCE(ds.amount, 0) AS amount
FROM SYSTEM_RANGE(1, 10)
LEFT JOIN daily_sales ds ON ds.sale_date = DATEADD('DAY', x - 1, DATE '2024-01-01')
ORDER BY date;
```

## LEFT JOIN 填充间隙 + COALESCE 填零


## 使用上述日期序列 LEFT JOIN 原始数据

COALESCE(amount, 0) 将 NULL 替换为 0

## 用最近已知值填充


COUNT 分组法模拟 IGNORE NULLS
WITH filled AS (
SELECT date, amount, COUNT(amount) OVER (ORDER BY date) AS grp
FROM date_series LEFT JOIN daily_sales ...
)
SELECT date, FIRST_VALUE(amount) OVER (PARTITION BY grp ORDER BY date) AS filled
FROM filled;
注意：H2 Database 的日期序列生成方式见上述特有语法
注意：使用 COALESCE 进行空值替换
注意：COUNT 分组法是模拟 IGNORE NULLS 的通用方案
